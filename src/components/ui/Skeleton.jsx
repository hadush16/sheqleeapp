// export default function Skeleton({ className = "className" }) {
//   return <div className={`skeleton bg-gray-200 ${className}`} />;
// }

export default function Skeleton({ className = "" }) {
  return <div className={`skeleton bg-gray-200 ${className}`} />;
}
